name: cdkloader

services:
  cdkloader:
    image: "${CDK_LOADER_IMAGE:-ghcr.io/hermitchen/cdkloader:latest}"
    ports:
      - "${CDK_LOADER_PORT:-1456}:1456"
    environment:
      DATABASE_URL: "${DATABASE_URL:-sqlite:////app/data/cdkloader.db}"
      ADMIN_PASSWORD: "${ADMIN_PASSWORD:-change-me}"
      ADMIN_TOKEN: "${ADMIN_TOKEN:-change-me-admin-token}"
      CREDENTIAL_SECRET: "${CREDENTIAL_SECRET:-development-credential-secret}"
      CDK_PEPPER: "${CDK_PEPPER:-development-cdk-pepper}"
      VALIDATION_MODE: "${VALIDATION_MODE:-remote}"
      VALIDATION_EGRESS_MODE: "${VALIDATION_EGRESS_MODE:-direct}"
      VALIDATION_PROBE_MODE: "${VALIDATION_PROBE_MODE:-fast}"
      VALIDATION_IMPERSONATE: "${VALIDATION_IMPERSONATE:-chrome146}"
      VALIDATION_TIMEOUT_SECONDS: "${VALIDATION_TIMEOUT_SECONDS:-30}"
      VALIDATION_ATTEMPTS: "${VALIDATION_ATTEMPTS:-3}"
      VALIDATION_PROXY: "${VALIDATION_PROXY:-}"
      VALIDATION_PROXY_POOL: "${VALIDATION_PROXY_POOL:-}"
      VALIDATION_RETRY_BASE_SECONDS: "${VALIDATION_RETRY_BASE_SECONDS:-2}"
      VALIDATION_RETRY_MAX_SECONDS: "${VALIDATION_RETRY_MAX_SECONDS:-30}"
      VALIDATION_RETRY_JITTER_SECONDS: "${VALIDATION_RETRY_JITTER_SECONDS:-0.5}"
      VALIDATION_GATE_THRESHOLD: "${VALIDATION_GATE_THRESHOLD:-5}"
      VALIDATION_COOLDOWN_SECONDS: "${VALIDATION_COOLDOWN_SECONDS:-60}"
      VALIDATION_CONCURRENCY: "${VALIDATION_CONCURRENCY:-2}"
      REDELIVERY_WINDOW_SECONDS: "${REDELIVERY_WINDOW_SECONDS:-1800}"
      PUBLIC_BASE_URL: "${PUBLIC_BASE_URL:-http://localhost:1456}"
      MAX_UPLOAD_BYTES: "${MAX_UPLOAD_BYTES:-100M}"
      OPERATION_LOG_RETENTION_DAYS: "${OPERATION_LOG_RETENTION_DAYS:-30}"
      EXPORT_RETENTION_SECONDS: "${EXPORT_RETENTION_SECONDS:-86400}"
      EXPORT_DIR: "${EXPORT_DIR:-/app/data/exports}"
    volumes:
      - ./data:/app/data
    init: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:1456/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
